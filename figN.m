function [grafico,freq] = figN(n)
    load databaseVectors.mat;
   switch n
       case 0
           grafico = y0_freq0;
           freq =  abs(y0_fft);

       case 1
            grafico = y0_freq0;
           freq =  abs(y0_fft);
       case 2
            grafico = y0_freq0;
           freq =  abs(y0_fft);
       case 3
            grafico = y0_freq0;
           freq =  abs(y0_fft);
       case 4
            grafico = y0_freq0;
           freq =  abs(y0_fft);
       case 5
            grafico = y0_freq0;
           freq =  abs(y0_fft);       
       case 6
            grafico = y0_freq0;
           freq =  abs(y0_fft);
       case 7
            grafico = y0_freq0;
           freq =  abs(y0_fft);
       case 8
            grafico = y0_freq0;
           freq =  abs(y0_fft);
       case 9
            grafico = y0_freq0;
           freq =  abs(y0_fft);
   end
end